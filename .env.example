CERT_PFX_PATH=/path/to/certificate.pfx
CERT_PFX_PASSWORD=your_password_here
# Optional: override default config/data paths (required if installed outside repo)
# EMISSOR_CONFIG_DIR=/path/to/config
# EMISSOR_DATA_DIR=/path/to/data
